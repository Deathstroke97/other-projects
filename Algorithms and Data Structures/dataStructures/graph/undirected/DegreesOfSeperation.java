package com.example.packages.dataStructures.graph.undirected;

import com.example.packages.stdlib.StdIn;

public class DegreesOfSeperation {
/*    public static void main(String[] args) {
        SymbolGraph sg = new SymbolGraph(args[0], args[1]);
        Graph G = sg.G();

        String sourse = args[2];
        if (!sg.contains(sourse)){
            System.out.println(sourse + " not in database");
            return;
        }
        int s = sg.index(sourse);
        BreadthFirstSearch bfs = new BreadthFirstSearch(G, s);
        while(!StdIn.isEmpty()){
            String sink = StdIn.readLine();
            if (sg.contains(sink)){
                int t = sg.index(sink);
                if (bfs.hasPathTo(t)){
                    for(int v : bfs.pathTo(t)){
                        //System.out.println(" " + sg.name(v));

                    }
                }

            }
            else {
                System.out.println("not connected");
            }
        }

    }*/
}
